{
  "devDependencies": {
    "@types/node": "22.*",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitepress": "^2.0.0-alpha.8"
  },
  "scripts": {
    "sync": "tsx ./syncRepositories.ts",
    "generate-single-page": "tsx ./generate-single-page.ts",
    "docs:dev": "npm run generate-single-page && vitepress dev",
    "docs:build": "npm run generate-single-page && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "dependencies": {
    "glob": "^11.0.3",
    "gray-matter": "^4.0.3"
  }
}
