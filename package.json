{
  "devDependencies": {
    "@types/node": "26.*",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2",
    "vitepress": "^2.0.0-alpha.18"
  },
  "scripts": {
    "sync": "tsx ./syncRepositories.ts",
    "generate-single-page": "tsx ./generate-single-page.ts",
    "generate-missing-single-pages": "tsx ./generate-missing-single-pages.ts",
    "docs:dev": "npm run generate-missing-single-pages && vitepress dev",
    "docs:build": "npm run generate-missing-single-pages && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "dependencies": {
    "glob": "^13.0.6",
    "gray-matter": "^4.0.3"
  }
}
